"use strict"; // we've written this to make the code know that i will write all the new stuff in js strictly means js ka new version.

// alert( 8 + 8 ) we are using node.js here and not a browser. iska output browser me aata hai yaha nhi.

console.log(3
           + 
            3) // vimro iss tarah se code mt krio, it should be readable.



console.log("Shivani")


let name = "Shivani"
let age = 16
let isLoggedIn = false
let state;

// these are types of datatypes

// number => 2 to the power of 53 (yaha tk numbers aa skte hai)
// bigint => used for facebook and all not for us mainly only for very big numbers
// string => ""
// boolean(haa ya naa, yaa toh wo logged in hai ya nhi hai, ya toh uska adhaar hai hampe ya nhi hai, etc,etc) => true/false
// null(also a standalone value i.e its a type of its own) means ki abhi value is not assigned
// undefined => 
// symbol => to find uniqness of individual components 

// object =>  will be talked in further vids

console.log(typeof undefined);  // iska output will be undefined cause its a type
console.log(typeof null);  //  but isla output will be object cause its a type of object


